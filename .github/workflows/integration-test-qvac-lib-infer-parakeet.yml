name: Run Integration Tests (Parakeet)

on:
  workflow_dispatch:
    inputs:
      ref:
        description: "Git ref (branch/tag/SHA) to test"
        type: string
        required: false
        default: main
      prebuild_package:
        description: "NPM package containing prebuilds (e.g. @qvac/transcription-parakeet@1.0.0)"
        required: true
        type: string
      workdir:
        description: "Relative path to package directory in monorepo"
        type: string
        required: false
        default: "packages/qvac-lib-infer-parakeet"
  workflow_call:
    inputs:
      ref:
        description: 'ref'
        type: string
      repository:
        type: string
      workdir:
        description: "Relative path to package directory in monorepo"
        required: false
        default: "packages/qvac-lib-infer-parakeet"
        type: string

jobs:
  run-integration-tests:
    timeout-minutes: 60
    continue-on-error: true
    runs-on: ${{ matrix.os }}
    name: ${{ matrix.os }}-${{ matrix.platform }}-${{ matrix.arch }}-integration-tests
    env:
      GITHUB_TOKEN: ${{ secrets.PAT_TOKEN }}
    permissions:
      contents: read
      packages: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ai-run-ubuntu-22.04
            platform: linux
            arch: x64
          - os: ai-run-linux-gpu
            platform: linux
            arch: x64
          - os: ubuntu-24.04-arm64-private
            platform: linux
            arch: arm64
          - os: macos-14-xlarge
            platform: darwin
            arch: arm64
          - os: macos-15-large
            platform: darwin
            arch: x64
          - os: windows-2022
            platform: win32
            arch: x64

    steps:
      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: lts/*

      - name: Windows - enable git long paths
        if: ${{ matrix.platform == 'win32' }}
        shell: bash
        run: git config --system core.longpaths true

      - name: Checkout code
        uses: actions/checkout@v6
        with:
          repository: ${{ inputs.repository || github.repository }}
          ref: ${{ inputs.ref || github.ref }}
          token: ${{ secrets.PAT_TOKEN }}

      - name: Configure scoped registry for @tetherto and @qvac packages
        env:
          GPR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        shell: bash
        run: |
          echo "Configuring scoped registry for @tetherto and @qvac packages..."
          set -eu
          echo "Writing .npmrc for dual registry install…"
          cd ${{ inputs.workdir }}
          cat > .npmrc <<NPMRC
          always-auth=true
          registry=https://registry.npmjs.org/
          @qvac:registry=https://registry.npmjs.org/
          @tetherto:registry=https://npm.pkg.github.com/
          //registry.npmjs.org/:_authToken=${NPM_TOKEN}
          //npm.pkg.github.com/:_authToken=${GPR_TOKEN}
          NPMRC

          # Configure git for private repos
          if [ -n "${{ secrets.PAT_TOKEN }}" ]; then
            git config --global url."https://${{ secrets.PAT_TOKEN }}:@github.com/".insteadOf "https://github.com/"
            git config --global url."https://${{ secrets.PAT_TOKEN }}:@github.com/".insteadOf "git@github.com:"
          else
            git config --global url."https://${{ github.token }}:@github.com/".insteadOf "https://github.com/"
            git config --global url."https://${{ github.token }}:@github.com/".insteadOf "git@github.com:"
          fi

      - name: Install NPM dependencies
        working-directory: ${{ inputs.workdir }}
        run: |
          npm install
          npm install -g bare bare-make

      - name: Download prebuilds from artifact
        if: ${{ !inputs.prebuild_package }}
        uses: actions/download-artifact@v7
        with:
          path: ${{ inputs.workdir }}/prebuilds
          merge-multiple: true

      - name: Download prebuilds from package
        if: ${{ inputs.prebuild_package }}
        working-directory: ${{ inputs.workdir }}
        shell: bash
        run: |
          mkdir -p prebuilds
          npm pack ${{ inputs.prebuild_package }} --pack-destination /tmp
          tar -xzf /tmp/*.tgz -C /tmp
          cp -r /tmp/package/prebuilds/* prebuilds/

      - name: Rename prebuilds from tetherto_ to qvac_ prefix
        if: ${{ inputs.prebuild_package }}
        working-directory: ${{ inputs.workdir }}
        shell: bash
        run: |
          find prebuilds -depth -name 'tetherto_*' | while read -r file; do
            dir=$(dirname "$file")
            base=$(basename "$file")
            newbase=$(echo "$base" | sed 's/^tetherto_/qvac_/')
            mv "$file" "$dir/$newbase"
          done

      - name: Linux - install dependencies
        if: matrix.platform == 'linux' || matrix.platform == 'android'
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install -y libgomp1

      - name: Run integration test
        shell: bash
        working-directory: ${{ inputs.workdir }}
        run: npm run test:integration
        env:
          GITHUB_TOKEN: ${{ secrets.PAT_TOKEN }}
          LD_LIBRARY_PATH: ${{ github.workspace }}/${{ inputs.workdir }}/prebuilds/${{ matrix.platform }}-${{ matrix.arch }}
          DYLD_LIBRARY_PATH: ${{ github.workspace }}/${{ inputs.workdir }}/prebuilds/${{ matrix.platform }}-${{ matrix.arch }}

      - name: Run RTF benchmark
        if: always()
        continue-on-error: true
        shell: bash
        working-directory: ${{ inputs.workdir }}
        run: npm run test:benchmark:rtf
        env:
          GITHUB_TOKEN: ${{ secrets.PAT_TOKEN }}
          LD_LIBRARY_PATH: ${{ github.workspace }}/${{ inputs.workdir }}/prebuilds/${{ matrix.platform }}-${{ matrix.arch }}
          DYLD_LIBRARY_PATH: ${{ github.workspace }}/${{ inputs.workdir }}/prebuilds/${{ matrix.platform }}-${{ matrix.arch }}

      - name: Upload RTF results
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@v6
        with:
          name: rtf-results-${{ matrix.os }}-${{ matrix.platform }}-${{ matrix.arch }}
          path: ${{ inputs.workdir }}/benchmarks/results/rtf-benchmark-*.json
          retention-days: 30
          if-no-files-found: ignore

      - name: Add RTF summary
        if: always()
        continue-on-error: true
        shell: bash
        working-directory: ${{ inputs.workdir }}
        run: |
          RESULT_FILE="benchmarks/results/rtf-benchmark-${{ matrix.platform }}-${{ matrix.arch }}.json"

          if [ -f "$RESULT_FILE" ]; then
            echo "### RTF — ${{ matrix.platform }}-${{ matrix.arch }}" >> $GITHUB_STEP_SUMMARY
            node -e "
              const r = JSON.parse(require('fs').readFileSync('${RESULT_FILE}', 'utf8'));
              const s = r.summary;
              const lines = [
                '| Metric | Mean | P50 | P95 |',
                '|--------|------|-----|-----|',
                '| RTF | ' + s.rtf.mean.toFixed(4) + ' | ' + s.rtf.p50.toFixed(4) + ' | ' + s.rtf.p95.toFixed(4) + ' |',
                '| Wall (ms) | ' + s.wallMs.mean.toFixed(0) + ' | ' + s.wallMs.p50.toFixed(0) + ' | ' + s.wallMs.p95.toFixed(0) + ' |',
                '| Tokens/s | ' + s.tokensPerSecond.mean.toFixed(1) + ' | ' + s.tokensPerSecond.p50.toFixed(1) + ' | ' + s.tokensPerSecond.p95.toFixed(1) + ' |',
                '',
              ];
              console.log(lines.join('\n'));
            " >> $GITHUB_STEP_SUMMARY
          fi
