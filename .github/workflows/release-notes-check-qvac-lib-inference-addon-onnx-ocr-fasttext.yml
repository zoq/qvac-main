name: Release Notes Check (OCR)

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
    branches:
      - main
      - release-*
    paths:
      - "packages/qvac-lib-inference-addon-onnx-ocr-fasttext/**"

permissions:
  contents: read
  pull-requests: read

jobs:
  release-notes-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout PR head
        uses: actions/checkout@v6
        with:
          repository: ${{ github.event.pull_request.head.repo.full_name }}
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*

      - name: Read PR package version
        id: package-version
        shell: bash
        run: |
          set -euo pipefail
          version=$(jq -r '.version' packages/qvac-lib-inference-addon-onnx-ocr-fasttext/package.json)
          echo "version=${version}" >> "$GITHUB_OUTPUT"

      - name: Checkout base repo
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.base.sha }}
          fetch-depth: 0

      - name: Detect version bump
        id: version
        uses: ./.github/actions/detect-version-bump
        with:
          current_version: ${{ steps.package-version.outputs.version }}
          prev_sha: ${{ github.event.pull_request.base.sha }}
          workdir: packages/qvac-lib-inference-addon-onnx-ocr-fasttext

      - name: Validate Release Notes on version bump
        if: steps.version.outputs.bumped == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const pullNumber = context.payload.pull_request.number;
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const version = "${{ steps.package-version.outputs.version }}";
            const expected = `packages/qvac-lib-inference-addon-onnx-ocr-fasttext/release-notes/v${version}.md`;

            const files = await github.paginate(
              github.rest.pulls.listFiles,
              { owner, repo, pull_number: pullNumber, per_page: 100 }
            );

            const hasNotes = files.some(file => file.filename === expected);
            if (!hasNotes) {
              core.setFailed(`Missing release notes file in PR changes: ${expected}`);
            } else {
              core.info(`Release notes file found in PR changes: ${expected}`);
            }
